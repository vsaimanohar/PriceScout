{
    "build": {
        "builder": "DOCKERFILE",
        "dockerfilePath": "./backend/Dockerfile",
        "watchPatterns": [
            "./backend/package.json",
            "./backend/server.js",
            "./backend/src/**",
            "./backend/Dockerfile"
        ]
    },
    "deploy": {
        "startCommand": "npm start"
    }
}